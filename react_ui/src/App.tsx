import React from 'react';
import ChatBot from './components/ChatBot';

const App: React.FC = () => {
    return (
        <div className="min-h-screen">
            <ChatBot />
        </div>
    );
};

export default App;
